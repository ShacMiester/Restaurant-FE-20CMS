export interface PreMenu {
  title: string;
  details: string;
  viewMenuLabel: string;
  preFaceMenuItems: preMenuItems[];
}
export interface preMenuItems {
  title: string;
  image: string;
  description: string;
}
